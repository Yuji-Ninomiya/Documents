⑧ C++の文字列オブジェクトの生成として正しいものを答えよ。

c. string str = "Hello";


① C++クラス定義の説明として正しいものを答えよ。

a. 最後にセミコロン ; をつける必要がある。


⑤ Javaクラス定義の説明として正しいものを答えよ。

b. フィールドとメソッドの両方を定義することができる。


⑦ メンバ関数の説明として正しいものを答えよ。

a. メンバ関数は定義されたクラスに属する全てのオブジェクトから呼び出し可能である。


⑧ メンバ変数の説明として正しいものを答えよ。

a. メンバ変数の存在期間はオブジェクトの存在期間と同じである。


③ 以下のJavaコードを実行するとどのような表示が得られるか答えよ。

String x = new String("hello");
String y = x;
System.out.println(x == y);

b. true


⑧ 以下のC++コードを実行するとどのような表示が得られるか答えよ。

string x = "hello";
string y = x;
cout << (x == y) << endl;

b. 1


⑨ デストラクタの説明として正しいものを答えよ。

a. クラス名の前に~を付けた名前で定義する必要がある。


⑩ コンストラクタの説明として正しいものを答えよ。

b. 戻り値の型を定義することはできない。


② C++で以下クラスのオブジェクトpのメンバ変数の設定として正しいものを答えよ。

class Person {
public:
   int age;
};

a. p.age = 20;


③ C++で以下のクラスが定義されている場合、オブジェクトの生成として正しいものを答えよ。

class Person {
public:
   int age;
};

c. Person p;

④ C++で以下のクラスが定義されている場合、オブジェクトの生成として正しいものを答えよ。

class Person {
public:
   int age;
   Person() { age = 20; }
};

c. Person p = Person();


⑥ Javaで以下クラスのオブジェクトpのメンバ変数の参照として正しいものを答えよ。

class Person {
   int age;
   int getAge() { return age; }
}

b. p.getAge();


⑦ Javaで以下のクラスが定義されている場合、オブジェクトの生成として正しいものを答えよ。

class Person {
   int age;
   Person() { age = 20; }
}

c. Person p = new Person();


オブジェクトの生成  --> クラス オブジェクト名 = _____
メンバ変数の設定 --> オブジェクト名.変数名


④ C++の名前空間の説明として正しいものを答えよ。

c. クラス名、関数名、変数名の重複を解消できる。


⑥ Javaのパッケージの説明として正しいものを答えよ。

a. クラス名を一意に識別するために利用する。


⑦ C++のprivate指定子の説明として正しいものを答えよ。

a. オブジェクト内でのみアクセス可能なメンバを指定する。


⑨ Javaのstatic修飾子の使い方として正しいものを答えよ。

c. クラス固有のメンバを定義する。





10
